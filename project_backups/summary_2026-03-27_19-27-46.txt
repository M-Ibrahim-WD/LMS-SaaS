Implemented a super admin and delegated admin-permissions system. Added AdminPermission enum and User fields isSuperAdmin plus adminPermissions. Auth tokens and user/session payloads now include these fields, and JWT validation refreshes them from the database. Admin backend actions are now permission-gated server-side: overview, tenants, tenant management, students, instructors, user management, courses, payments, plans, and admin-account review. Added super-admin-only admin-account management endpoints to create limited admin accounts, update their permissions, and activate/deactivate them. Protected the super admin account so it is excluded from managed admin lists and cannot be viewed, edited, or deactivated through admin-management flows. Promoted admin@example.com to the platform super admin with full permissions and no tenant. Updated the admin frontend to show a Super Admin Controls area for creating and managing limited admin accounts, and to hide admin sections the current admin lacks permission to use. Validation passed: prisma schema pushed, backend build, frontend lint, frontend build.
