Task summary: Roadmap execution pass 5 (richer course metadata and discovery filters)

Prisma models added/modified:
- Modified `Course` model in `apps/backend/prisma/schema.prisma`:
  - added `category` (String, optional)
  - added `level` (CourseLevel enum, default BEGINNER)
- Added new Prisma enum `CourseLevel`:
  - `BEGINNER`
  - `INTERMEDIATE`
  - `ADVANCED`
- Added indexes on `Course.category` and `Course.level`

New functions, APIs, or routes:
- No new routes were added.
- Extended existing `GET /api/courses` to support optional server-side discovery query parameters via new DTO `apps/backend/src/modules/courses/dto/course-query.dto.ts`:
  - `search`
  - `pricing` (`ALL | FREE | PAID`)
  - `category`
  - `level`
  - `page`
  - `pageSize`
- Updated `CoursesController.getAll(...)` in `apps/backend/src/modules/courses/controllers/courses.controller.ts` to accept the query DTO.
- Added backend helper logic in `CoursesService`:
  - `buildCourseWhere(...)`
  - `resolvePagination(...)`
  These apply role-aware filtering plus discovery filters/pagination.

Behavior and backend logic changes:
- Course create/update now support metadata:
  - `category`
  - `level`
- `CoursesService.create(...)` now stores trimmed `category` and selected `level`.
- `CoursesService.update(...)` now updates `category` and `level` and normalizes blank category to `null`.
- `CoursesService.getAllByTenant(...)` now supports:
  - title/description/category search
  - free/paid filtering
  - category filtering
  - level filtering
  - page/pageSize pagination
- Discovery remains role-aware:
  - instructor sees own tenant courses
  - admin sees current tenant courses
  - student sees published courses only from followed instructors

Frontend changes:
- Instructor course management page `apps/frontend/src/app/instructor/courses/page.tsx`:
  - create form now includes `category` input
  - create form now includes `level` select
  - course list now displays category and level metadata
- Courses discovery page `apps/frontend/src/app/courses/page.tsx`:
  - added search input
  - added pricing filter
  - added category filter
  - added level filter
  - added page navigation (Previous / Next)
  - uses backend query parameters instead of only local filtering
  - course cards now display category and level badges
- Dashboard student course cards `apps/frontend/src/app/dashboard/_components/student-dashboard-section.tsx`:
  - now display category and level badges
- Dashboard course types `apps/frontend/src/app/dashboard/_components/dashboard-types.ts`:
  - updated `StudentCourse` to include `category` and `level`
- Course details page `apps/frontend/src/app/courses/[id]/page.tsx`:
  - now includes category and level metadata in both list/detail interfaces
  - displays category and level badges near the course heading

Migrations or schema changes:
- Prisma schema changed.
- Applied with:
  - `pnpm --filter @lms/backend exec prisma validate`
  - `pnpm --filter @lms/backend exec prisma db push --accept-data-loss`
  - `pnpm --filter @lms/backend exec prisma generate`
- Note: `db push` hit the usual Windows Prisma DLL rename lock during auto-generate, so a follow-up generate completed successfully with `PRISMA_GENERATE_NO_ENGINE=1`.

Validation executed:
- `pnpm --filter @lms/backend exec prisma validate` passed.
- `pnpm --filter @lms/backend exec prisma db push --accept-data-loss` applied schema successfully.
- `pnpm --filter @lms/backend exec prisma generate` passed after follow-up run.
- `pnpm --filter @lms/backend test` passed.
- `pnpm --filter @lms/backend build` passed.
- `pnpm --filter @lms/frontend lint` passed.
- `pnpm --filter @lms/frontend build` passed.

Notes:
- This pass covers two roadmap items together:
  - richer course metadata
  - server-side course discovery filtering/pagination support
- Remaining roadmap work is now primarily activity/history surfaces and additional UI polish rather than structural platform gaps.
