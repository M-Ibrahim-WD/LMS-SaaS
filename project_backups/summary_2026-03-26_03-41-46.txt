Phase 1 Admin and SaaS operations completed.

Prisma schema changes:
- Added Tenant moderation fields in apps/backend/prisma/schema.prisma:
  - isActive Boolean @default(true) mapped to is_active
  - deactivatedAt DateTime? mapped to deactivated_at
  - @@index([isActive])
- Added User moderation fields in apps/backend/prisma/schema.prisma:
  - isActive Boolean @default(true) mapped to is_active
  - deactivatedAt DateTime? mapped to deactivated_at
  - @@index([isActive])
- Schema was validated successfully with Prisma validate.
- Database schema changes for the new moderation fields had already been applied earlier in this interrupted task with prisma db push --accept-data-loss.

Backend changes:
- Added new admin module:
  - apps/backend/src/modules/admin/admin.module.ts
  - apps/backend/src/modules/admin/controllers/admin.controller.ts
  - apps/backend/src/modules/admin/services/admin.service.ts
  - apps/backend/src/modules/admin/dto/admin-query.dto.ts
- Registered AdminModule in apps/backend/src/app.module.ts.
- Added admin-only protected endpoints under /api/admin:
  - GET /api/admin/overview
  - GET /api/admin/tenants
  - GET /api/admin/tenants/:id
  - PATCH /api/admin/tenants/:id/status
  - GET /api/admin/users
  - GET /api/admin/users/:id
  - PATCH /api/admin/users/:id/status
  - GET /api/admin/courses
  - GET /api/admin/payments
  - GET /api/admin/activity
- AdminService now provides platform-wide oversight for tenants, users, courses, payments, and recent activity.
- UsersService was extended to expose moderation-aware profile selection and soft activate/deactivate support via setActiveStatus(userId, isActive).
- TenantsService was extended with setActiveStatus(tenantId, isActive) for soft activation/deactivation.
- AuthService login now blocks inactive users and users in deactivated tenants with clear UnauthorizedException messages.
- JwtStrategy now validates active user and active tenant state on authenticated requests and rejects deactivated accounts/workspaces.

Frontend changes:
- Added admin route protection in apps/frontend/src/middleware.ts for /admin and restricted it to ADMIN role.
- Added admin entry point on apps/frontend/src/app/dashboard/page.tsx with admin-only console CTA.
- Added new admin dashboard page at apps/frontend/src/app/admin/page.tsx.
- The admin page includes:
  - overview stat cards
  - tenant list and tenant detail panel
  - user list and user detail panel
  - platform course oversight
  - platform payment oversight
  - recent platform activity
  - activate/deactivate mutations for users and tenants

Testing and validation:
- Added direct backend test coverage for the new admin service in apps/backend/src/modules/admin/services/admin.service.spec.ts.
- The new tests cover:
  - overview aggregation
  - tenant usage mapping
  - user detail profile-image resolution and recent activity
  - user activation/deactivation delegation
- Validation completed successfully:
  - pnpm --filter @lms/backend test
  - pnpm --filter @lms/backend build
  - pnpm --filter @lms/frontend lint
  - pnpm --filter @lms/frontend build
  - pnpm --filter @lms/backend exec prisma validate

Notes:
- Temporary smoke-test harness tmp_admin_smoke.ps1 was removed after validation cleanup.
- Live admin API smoke testing was partially attempted during recovery from the interrupted session, but Windows/OneDrive local process-launch behavior made the detached smoke harness unreliable. The implemented admin feature set was instead hardened with direct backend tests plus successful production frontend/backend validation.
