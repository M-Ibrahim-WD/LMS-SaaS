Task: Implement multi-instructor support for students.

Prisma models added/modified:
- Added new model in apps/backend/prisma/schema.prisma:
  - StudentInstructor
    - id
    - studentId mapped to student_id
    - instructorId mapped to instructor_id
    - createdAt
- Added unique constraint on StudentInstructor:
  - @@unique([studentId, instructorId])
- Added indexes on StudentInstructor:
  - @@index([studentId])
  - @@index([instructorId])
- Updated User model relations:
  - joinedInstructors relation for student-side links
  - followedByStudents relation for instructor-side reverse links
- Updated RegisterDto to support student inviteCode during registration.

New functions, APIs, or routes:
- Added new backend module:
  - apps/backend/src/modules/student-instructors/student-instructors.module.ts
- Added new DTO:
  - apps/backend/src/modules/student-instructors/dto/join-student-instructor.dto.ts
- Added new service:
  - apps/backend/src/modules/student-instructors/services/student-instructors.service.ts
- Added new controller:
  - apps/backend/src/modules/student-instructors/controllers/student-instructors.controller.ts
- Added new endpoints:
  - POST /api/student/instructors/join
  - GET /api/student/instructors

Backend business logic changes:
- Student registration now requires inviteCode.
- On student registration:
  - backend creates the student account
  - resolves the instructor via tenant invite code
  - creates StudentInstructor relation
  - sets student tenantId only if tenantId was null, to preserve legacy compatibility for existing guarded flows
- Existing POST /api/tenants/join now uses the same StudentInstructor relation logic for backward compatibility.
- Student join endpoint prevents duplicates and throws:
  - Already
