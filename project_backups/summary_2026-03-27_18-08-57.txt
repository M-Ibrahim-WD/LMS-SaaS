Resolved the persistent uploaded-image display issue more deeply. Added cross-origin resource policy support for backend-served media by configuring Helmet with crossOriginResourcePolicy: cross-origin and explicitly setting Cross-Origin-Resource-Policy: cross-origin on profile-image and course-thumbnail streaming responses. Also improved frontend upload UX so successful profile-image and thumbnail uploads immediately keep and apply the returned image URL instead of briefly falling back to stale local state. Validation passed: backend build and frontend lint. Backend server must be restarted for the new image-serving headers to take effect.
