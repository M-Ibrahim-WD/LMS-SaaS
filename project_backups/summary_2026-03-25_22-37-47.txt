Task Summary: TikTok Profile Redesign Follow-up Validation and Fixes

Prisma models added/modified in this follow-up:
- No additional Prisma model changes beyond the prior profile redesign schema.

New functions, APIs, or routes in this follow-up:
- No new routes added in this follow-up.
- Confirmed live behavior for:
  - GET /api/instructor/public/:id
  - GET /api/instructor/public/:id/reviews
  - /instructors/[id] frontend page
  - PATCH /api/users/profile with profile redesign settings flow

Code fixes applied in this follow-up:
- Fixed profile settings submission in frontend so optional fields do not send empty strings that trigger backend validation errors.
- Updated profile save payload in:
  - apps/frontend/src/app/profile/page.tsx
  - fullName is trimmed
  - bio is omitted when empty
  - profileImage is omitted when empty
- Verified student dashboard instructor cards link to the new public instructor profile route.

Validation and smoke test results:
- Frontend TypeScript lint: passed
- Live smoke test passed:
  - instructor registration: 201
  - profile update save: 200
  - public instructor profile API: 200
  - public instructor reviews API: 200
  - public instructor page route /instructors/[id]: 200
- Public profile returned expected data:
  - id
  - fullName
  - bio
  - tenant
  - stats
  - courses
  - reviews

Environment/runtime notes:
- The frontend dev server recovered from the earlier broken .next manifest state.
- /profile and /instructors/[id] now render successfully in the live dev environment.
