Task: Prevent duplicate payment accounts per instructor and method while allowing multiple payment methods.

Prisma models added/modified:
- Modified PaymentMethod model in apps/backend/prisma/schema.prisma.
- Kept existing soft delete fields:
  - isDeleted Boolean mapped to is_deleted
  - deletedAt DateTime? mapped to deleted_at
- Added unique constraint:
  - @@unique([instructorId, type, details, isDeleted])
- This makes account uniqueness scoped to:
  - instructor_id + method type + account value(details) + isDeleted

New functions, APIs, or routes:
- No new API routes were added.
- Updated backend create logic in apps/backend/src/modules/payment-methods/services/payment-methods.service.ts.
- Updated frontend form logic in apps/frontend/src/app/dashboard/page.tsx.

Backend business logic implemented:
- Instructor can add multiple payment methods.
- Same method type can be added multiple times when the account details differ.
- Same account details cannot be added twice for the same method type by the same instructor while isDeleted = false.
- Before create, backend now checks for an existing active record with:
  - same instructorId
  - same tenantId
  - same type
  - same normalized details value
  - isDeleted = false
- If found, backend throws:
  - This
