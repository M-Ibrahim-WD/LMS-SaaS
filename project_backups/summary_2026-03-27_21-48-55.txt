Implemented the next super-admin hardening pass. Added delegated-admin forced password change support with a new User.mustChangePassword field, JWT/session propagation, a protected POST-login password-change flow at /admin/change-password, and a new backend auth change-password endpoint that refreshes the access token after the password is changed. Delegated admins created by the super admin and delegated admins whose passwords are reset are now marked to change their password on first login. Added super-admin audit-log filtering by category (Admins, Plans, Tenants, Users) in the backend and admin UI. Added clone-permissions-from-existing-admin support in the super-admin admin-creation form. Improved delegated-admin management UI with password-change status badges and clearer temporary-password messaging. Hardened the frontend build script so it no longer tries to delete .next-build before building, avoiding the recurring OneDrive/Windows lock. Fixed backend unit tests affected by the new auth/admin behavior. Validation results: prisma validate passed, prisma db push passed, backend test passed, backend build passed, frontend lint passed, frontend build passed. Note: explicit prisma generate still hits a Windows EPERM rename lock on Prisma's query_engine binary in node_modules, but the project builds and tests successfully with the current generated client state.
