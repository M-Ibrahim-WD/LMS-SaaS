Fixed three admin/media issues. 1) Repaired uploaded media URLs so saved profile images and course thumbnails now resolve through /api/users/:id/profile-image and /api/courses/:id/thumbnail instead of broken non-API paths. 2) Reworked the admin subscription-plan form into a guided one-section-at-a-time flow with sequential steps: Identity, Pricing, Workspace Limits, Course Delivery, Teaching Tools, Insights and Media, and Plan Status, while keeping the final quick summary and create/update action at the end. 3) Removed platform admins from the normal Users list by default so the admin account is treated as a separate platform account rather than a regular instructor/student user. Validation passed: backend build and frontend lint. Frontend production build is still blocked by the known local OneDrive .next trace file-lock (EPERM), not by a TypeScript or app-code error.
