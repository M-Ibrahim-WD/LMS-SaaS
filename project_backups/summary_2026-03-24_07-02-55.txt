Task summary: Roadmap execution pass 6 (activity/history surfaces)

Prisma models added/modified:
- No Prisma models were added or modified in this pass.
- No schema fields, relations, enums, indexes, or constraints changed.

New functions, APIs, or routes:
- No backend or frontend API routes were added.
- Added reusable frontend activity component:
  - `apps/frontend/src/components/activity-feed.tsx`
    - renders a structured recent-activity timeline with empty state and severity tones

Behavior and UI changes:
- Updated `apps/frontend/src/app/profile/page.tsx` to add role-based recent activity sections using existing APIs.

Student recent activity sources on Profile:
- `GET /student/instructors`
  - used to show recent instructor joins
- `GET /enrollments/my-courses`
  - used to show recent course enrollments
- `GET /payments/my`
  - used to show recent payment submissions and status updates

Instructor recent activity sources on Profile:
- `GET /courses`
  - used to show recent course creation/publish/update activity
- `GET /payments/instructor`
  - used to show recent student payment activity

UI behavior:
- Students now see a `Recent Activity` feed in Profile showing latest:
  - instructor joins
  - enrollments
  - payment statuses
- Instructors now see a `Recent Activity` feed in Profile showing latest:
  - course activity
  - student payment activity
- Activity items are sorted newest-first and limited to the most recent entries for readability.
- Activity cards use tone styling:
  - neutral
  - success
  - warning

Files added/modified:
- Added:
  - `apps/frontend/src/components/activity-feed.tsx`
- Modified:
  - `apps/frontend/src/app/profile/page.tsx`

Migrations or schema changes:
- None in this pass.
- No Prisma migration or db push was required.

Validation executed:
- `pnpm --filter @lms/frontend lint` passed.
- `pnpm --filter @lms/frontend build` passed.

Notes:
- This pass completes the activity/history surface item from the roadmap using existing data rather than introducing a new activity log table.
- Remaining work from the original roadmap is now largely additional polish or future product expansion rather than missing structural foundations.
