Task summary: Roadmap execution pass 4 (UI/page-shell and feedback refinement)

Prisma models added/modified:
- No Prisma models were added or modified in this pass.
- No schema fields, relations, enums, indexes, or constraints changed.

New functions, APIs, or routes:
- No backend or frontend API routes were added.
- Added reusable frontend UI primitives:
  - `apps/frontend/src/components/page-shell.tsx`
    - standardized page heading, description, optional back navigation, and action area
  - `apps/frontend/src/components/status-banner.tsx`
    - standardized info/error/success status presentation
  - `apps/frontend/src/components/auth-panel.tsx`
    - standardized auth-page layout with title, description, content area, and footer

Behavior and UI changes:
- `apps/frontend/src/app/courses/page.tsx`
  - now uses `PageShell` for consistent page framing
  - now uses `StatusBanner` for loading/error feedback
  - retains `ContentCard` and `EmptyState` usage with cleaner structure
- `apps/frontend/src/app/my-courses/page.tsx`
  - now uses `PageShell`
  - now uses `StatusBanner` for loading/error feedback
  - keeps reusable `ContentCard` and `EmptyState`
- `apps/frontend/src/app/profile/page.tsx`
  - now uses `PageShell`
  - now uses `StatusBanner` for loading/error feedback
- `apps/frontend/src/app/instructor/courses/page.tsx`
  - now uses `PageShell`
  - now uses `ContentCard` for the create/manage surfaces
  - now uses `StatusBanner` for load/create feedback
- `apps/frontend/src/app/login/page.tsx`
  - now uses shared `AuthPanel`
  - now uses shared `StatusBanner` for errors
- `apps/frontend/src/app/register/page.tsx`
  - now uses shared `AuthPanel`
  - now uses shared `StatusBanner` for errors

Overall UX improvements:
- page headers, descriptions, back-navigation placement, and action placement are now more consistent across the main completed pages
- loading and error feedback is more visually consistent instead of mixing plain text with ad hoc styling
- login/register now share a common visual structure instead of being separate one-off forms

Migrations or schema changes:
- None in this pass.
- No Prisma migration or db push was required.

Validation executed:
- `pnpm --filter @lms/frontend lint` passed.
- `pnpm --filter @lms/frontend build` passed.

Notes:
- This pass addresses the reusable page-shell / feedback-state cleanup portion of the roadmap.
- Remaining roadmap work is now mostly later product enhancements rather than structural cleanup:
  - richer metadata
  - server-side filtering/pagination
  - activity/history views
  - further visual polish across less-touched pages
