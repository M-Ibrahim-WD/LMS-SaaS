Fixed the media URL issue for uploaded profile images and course thumbnails by returning absolute backend asset URLs instead of relative paths, so images load from the API server after save. Updated users and courses services to build media URLs from PUBLIC_SERVER_URL when available, or default to http://localhost:4000. Validation passed: backend build, frontend lint, and frontend production build. Also preserved the one-step-at-a-time admin subscription plan wizard from the previous pass, with only the current step visible and the final summary appearing on the last step.
